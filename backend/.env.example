# DB
DB_HOST=localhost
DB_PORT=5432
DB_USER=dummy
DB_PASSWORD=dev_password_123
DB_NAME=dummy_dev
DB_SSL=false

# 인증 — 원본에는 누락되어 있던 키. 더미에서는 반드시 정의!
# 부팅 시 미정의면 즉시 실패하므로 반드시 채우세요.
JWT_SECRET=

# 서버
NODE_ENV=local
PORT=3001

# CORS (콤마 구분, 선택)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
